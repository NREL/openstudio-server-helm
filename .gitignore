.idea

# Secrets file - generated from template, do not commit
openstack/values-openstack.yaml

# Local operational/remediation state - never commit
.health-remediation-state/
# Agent tool artifacts - never commit
.agents/
# Agent per-session execution logs (rendered to repo root by some
# orchestration tools; canonical home is .agents/results/).
result-*.md
