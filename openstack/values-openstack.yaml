# OpenStack-only overrides for openstudio-server.
# Keep this file limited to values that differ from openstudio-server/values.yaml.

global:
  provider:
    name: "openstack"

nfs-server-provisioner:
  storageClass:
    mountOptions:
      - vers=4
      - sync

db:
  persistence:
    storageClass: "csi-cinder"

redis:
  persistence:
    storageClass: "csi-cinder"
