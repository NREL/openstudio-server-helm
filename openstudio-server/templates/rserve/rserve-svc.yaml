apiVersion: v1
kind: Service
metadata:
  name: {{ include "openstudio.rserveServiceName" . }}
spec:
  selector:
    app: {{ default "rserve" .Values.rserve.label }}
    release: {{ .Release.Name }}
  ports:
    - name: rserve
      protocol: TCP
      port: {{ default 6311 .Values.rserve_svc.port }}
